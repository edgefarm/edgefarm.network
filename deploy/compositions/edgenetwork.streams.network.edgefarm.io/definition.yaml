## WARNING: This file was autogenerated!
## Manual modifications will be overwritten
## unless ignore: true is set in generate.yaml!
## Last Modification: 20:57:28 on 03-29-2023.

apiVersion: apiextensions.crossplane.io/v1
kind: CompositeResourceDefinition
metadata:
  name: xedgenetworks.streams.network.edgefarm.io
spec:
  claimNames:
    kind: EdgeNetwork
    plural: edgenetworks
  group: streams.network.edgefarm.io
  names:
    categories:
      - crossplane
      - composition
      - streams
    kind: XEdgeNetwork
    plural: xedgenetworks
  versions:
    - additionalPrinterColumns:
        - jsonPath: .spec.network
          name: NETWORK
          type: string
        - jsonPath: .metadata.creationTimestamp
          name: AGE
          type: date
        - jsonPath: .spec.forProvider.manifest.status.desired
          name: DESIRED
          type: string
        - jsonPath: .spec.forProvider.manifest.status.current
          name: CURRENT
          type: string
        - jsonPath: .spec.forProvider.manifest.status.ready
          name: READY
          type: string
        - jsonPath: .spec.forProvider.manifest.spec.limits.fileStorage
          name: FILE LIMIT
          priority: 1
          type: string
        - jsonPath: .spec.forProvider.manifest.spec.limits.inMemoryStorage
          name: MEMORY LIMIT
          priority: 1
          type: string
      name: v1alpha1
      referenceable: true
      schema:
        openAPIV3Schema:
          properties:
            spec:
              description: The spec to define an edge network
              properties:
                connectionSecretRefs:
                  description: The connection secrets to connect to the network
                  properties:
                    sysAccountUserSecretRef:
                      description:
                        Reference to the secret containing the credentials
                        to connect to the network as a system account user
                      properties:
                        name:
                          description:
                            "Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            TODO: Add other useful fields. apiVersion, kind, uid?"
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    systemUserSecretRef:
                      description:
                        Reference to the secret containing the credentials
                        to connect to the network as a system user
                      properties:
                        name:
                          description:
                            "Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            TODO: Add other useful fields. apiVersion, kind, uid?"
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                  required:
                    - sysAccountUserSecretRef
                    - systemUserSecretRef
                  type: object
                limits:
                  description: Hardware limits for the edge network
                  properties:
                    fileStorage:
                      default: 1G
                      description:
                        How much disk space is available for data that is
                        stored on disk
                      pattern: ^\d+[GMKB]?$
                      type: string
                    inMemoryStorage:
                      default: 1G
                      description:
                        How much memory is available for data that is stored
                        in memory
                      pattern: ^\d+[GMKB]?$
                      type: string
                  required:
                    - fileStorage
                    - inMemoryStorage
                  type: object
                network:
                  description: The name of the network
                  type: string
                nodeSelectorTerm:
                  description:
                    Indicates the node selector to form the node pool. A
                    pool's nodeSelectorTerm is not allowed to be updated.
                  properties:
                    matchExpressions:
                      description: A list of node selector requirements by node's labels.
                      items:
                        description:
                          A node selector requirement is a selector that
                          contains values, a key, and an operator that relates the key
                          and values.
                        properties:
                          key:
                            description: The label key that the selector applies to.
                            type: string
                          operator:
                            description:
                              Represents a key's relationship to a set of
                              values. Valid operators are In, NotIn, Exists, DoesNotExist.
                              Gt, and Lt.
                            type: string
                          values:
                            description:
                              An array of string values. If the operator
                              is In or NotIn, the values array must be non-empty. If
                              the operator is Exists or DoesNotExist, the values array
                              must be empty. If the operator is Gt or Lt, the values
                              array must have a single element, which will be interpreted
                              as an integer. This array is replaced during a strategic
                              merge patch.
                            items:
                              type: string
                            type: array
                        required:
                          - key
                          - operator
                        type: object
                      type: array
                    matchFields:
                      description: A list of node selector requirements by node's fields.
                      items:
                        description:
                          A node selector requirement is a selector that
                          contains values, a key, and an operator that relates the key
                          and values.
                        properties:
                          key:
                            description: The label key that the selector applies to.
                            type: string
                          operator:
                            description:
                              Represents a key's relationship to a set of
                              values. Valid operators are In, NotIn, Exists, DoesNotExist.
                              Gt, and Lt.
                            type: string
                          values:
                            description:
                              An array of string values. If the operator
                              is In or NotIn, the values array must be non-empty. If
                              the operator is Exists or DoesNotExist, the values array
                              must be empty. If the operator is Gt or Lt, the values
                              array must have a single element, which will be interpreted
                              as an integer. This array is replaced during a strategic
                              merge patch.
                            items:
                              type: string
                            type: array
                        required:
                          - key
                          - operator
                        type: object
                      type: array
                  type: object
                  x-kubernetes-map-type: atomic
                subNetwork:
                  description: The name of the sub network
                  type: string
                tolerations:
                  description:
                    Indicates the tolerations the pods under this pool have.
                    A pool's tolerations is not allowed to be updated.
                  items:
                    description:
                      The pod this Toleration is attached to tolerates any
                      taint that matches the triple <key,value,effect> using the matching
                      operator <operator>.
                    properties:
                      effect:
                        description:
                          Effect indicates the taint effect to match. Empty
                          means match all taint effects. When specified, allowed values
                          are NoSchedule, PreferNoSchedule and NoExecute.
                        type: string
                      key:
                        description:
                          Key is the taint key that the toleration applies
                          to. Empty means match all taint keys. If the key is empty,
                          operator must be Exists; this combination means to match all
                          values and all keys.
                        type: string
                      operator:
                        description:
                          Operator represents a key's relationship to the
                          value. Valid operators are Exists and Equal. Defaults to Equal.
                          Exists is equivalent to wildcard for value, so that a pod
                          can tolerate all taints of a particular category.
                        type: string
                      tolerationSeconds:
                        description:
                          TolerationSeconds represents the period of time
                          the toleration (which must be of effect NoExecute, otherwise
                          this field is ignored) tolerates the taint. By default, it
                          is not set, which means tolerate the taint forever (do not
                          evict). Zero and negative values will be treated as 0 (evict
                          immediately) by the system.
                        format: int64
                        type: integer
                      value:
                        description:
                          Value is the taint value the toleration matches
                          to. If the operator is Exists, the value should be empty,
                          otherwise just a regular string.
                        type: string
                    type: object
                  type: array
              required:
                - connectionSecretRefs
                - limits
                - network
                - subNetwork
              type: object
            status:
              properties:
                current:
                  description: The amount of current participants in the edge network
                  type: string
                desired:
                  description: The amount of desired participants in the edge network
                  type: string
                observed:
                  description:
                    Freeform field containing information about the observed
                    status.
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                ready:
                  description: The amount of ready participants in the edge network
                  type: string
                uid:
                  description:
                    The unique ID of this EdgeNetwork resource reported by
                    the provider
                  type: string
              type: object
      served: true
