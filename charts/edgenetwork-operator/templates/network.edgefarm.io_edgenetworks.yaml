---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: edgenetworks.network.edgefarm.io
spec:
  group: network.edgefarm.io
  names:
    categories:
    - edgefarm
    kind: EdgeNetwork
    listKind: EdgeNetworkList
    plural: edgenetworks
    shortNames:
    - en
    singular: edgenetwork
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.network
      name: NETWORK
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .status.desired
      name: DESIRED
      type: string
    - jsonPath: .status.current
      name: CURRENT
      type: string
    - jsonPath: .status.ready
      name: READY
      type: string
    - jsonPath: .spec.limits.fileStorage
      name: FILE LIMIT
      priority: 1
      type: string
    - jsonPath: .spec.limits.inMemoryStorage
      name: MEMORY LIMIT
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: The spec to define an edge network
            properties:
              connectionSecretRefs:
                description: The connection secrets to connect to the network
                properties:
                  sysAccountUserSecretRef:
                    description: Reference to the secret containing the credentials
                      to connect to the network as a system account user
                    properties:
                      name:
                        description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          TODO: Add other useful fields. apiVersion, kind, uid?'
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  systemUserSecretRef:
                    description: Reference to the secret containing the credentials
                      to connect to the network as a system user
                    properties:
                      name:
                        description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          TODO: Add other useful fields. apiVersion, kind, uid?'
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - sysAccountUserSecretRef
                - systemUserSecretRef
                type: object
              limits:
                description: Hardware limits for the edge network
                properties:
                  fileStorage:
                    default: 1G
                    description: How much disk space is available for data that is
                      stored on disk
                    pattern: ^\d+[GMKB]?$
                    type: string
                  inMemoryStorage:
                    default: 1G
                    description: How much memory is available for data that is stored
                      in memory
                    pattern: ^\d+[GMKB]?$
                    type: string
                required:
                - fileStorage
                - inMemoryStorage
                type: object
              network:
                description: The name of the network
                type: string
              nodepoolSelector:
                description: NodePoolSelector is a label query over nodepool that
                  should match the replica count. It must match the nodepool's labels.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: A label selector requirement is a selector that
                        contains values, a key, and an operator that relates the key
                        and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: operator represents a key's relationship to
                            a set of values. Valid operators are In, NotIn, Exists
                            and DoesNotExist.
                          type: string
                        values:
                          description: values is an array of string values. If the
                            operator is In or NotIn, the values array must be non-empty.
                            If the operator is Exists or DoesNotExist, the values
                            array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: matchLabels is a map of {key,value} pairs. A single
                      {key,value} in the matchLabels map is equivalent to an element
                      of matchExpressions, whose key field is "key", the operator
                      is "In", and the values array contains only "value". The requirements
                      are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              subNetwork:
                description: The name of the sub network
                type: string
              tolerations:
                description: Indicates the tolerations the pods under this pool have.
                  A pool's tolerations is not allowed to be updated.
                items:
                  description: The pod this Toleration is attached to tolerates any
                    taint that matches the triple <key,value,effect> using the matching
                    operator <operator>.
                  properties:
                    effect:
                      description: Effect indicates the taint effect to match. Empty
                        means match all taint effects. When specified, allowed values
                        are NoSchedule, PreferNoSchedule and NoExecute.
                      type: string
                    key:
                      description: Key is the taint key that the toleration applies
                        to. Empty means match all taint keys. If the key is empty,
                        operator must be Exists; this combination means to match all
                        values and all keys.
                      type: string
                    operator:
                      description: Operator represents a key's relationship to the
                        value. Valid operators are Exists and Equal. Defaults to Equal.
                        Exists is equivalent to wildcard for value, so that a pod
                        can tolerate all taints of a particular category.
                      type: string
                    tolerationSeconds:
                      description: TolerationSeconds represents the period of time
                        the toleration (which must be of effect NoExecute, otherwise
                        this field is ignored) tolerates the taint. By default, it
                        is not set, which means tolerate the taint forever (do not
                        evict). Zero and negative values will be treated as 0 (evict
                        immediately) by the system.
                      format: int64
                      type: integer
                    value:
                      description: Value is the taint value the toleration matches
                        to. If the operator is Exists, the value should be empty,
                        otherwise just a regular string.
                      type: string
                  type: object
                type: array
            required:
            - connectionSecretRefs
            - limits
            - network
            - nodepoolSelector
            - subNetwork
            type: object
          status:
            properties:
              current:
                description: The amount of current participants in the edge network
                type: string
              desired:
                description: The amount of desired participants in the edge network
                type: string
              ready:
                description: The amount of ready participants in the edge network
                type: string
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
